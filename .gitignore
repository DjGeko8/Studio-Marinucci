node_modules/
.next/
out/
build/
.open-next/
.wrangler/
worker-configuration.d.ts
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
Thumbs.db

# Le fotografie provvisorie SONO versionate, per far vedere il sito completo in
# anteprima. Portano tutte la fascia impressa «PROVVISORIA — NON PUBBLICABILE» e
# `npm run verifica-immagini` blocca il go-live finché restano in uso.

# Skill Cloudflare per gli assistenti di sviluppo: strumenti di lavoro, non parte
# del sito. Si reinstallano con:
#   npx -y skills add cloudflare/skills --skill '*' --yes
# Se preferisci che chi clona il repository le trovi già pronte, togli queste righe.
.agents/
.claude/skills/
